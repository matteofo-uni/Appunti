#include <stdio.h>

int main(void) {
    int height = 21;
    double radius = 3.5;
    printf("Altezza %d e raggio %lf\n", height, radius);

    char letter = 'A';
    printf("La lettera %c ha valore ASCII %d\n", letter, letter);

    return 0;
}